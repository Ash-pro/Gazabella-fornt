meta {
  name: Orders without auth → 401
  type: http
  seq: 1
}

get {
  url: {{base_url}}/orders
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("returns 401 without token", function() {
    expect(res.status).to.equal(401);
  });
}
